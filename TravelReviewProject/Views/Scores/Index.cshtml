@model IEnumerable<TravelReviewProject.Models.Score>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReviewScore)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReviewScore)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ScoreID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ScoreID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ScoreID })
        </td>
    </tr>
}

</table>
